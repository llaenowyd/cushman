<!-- Anchor.stories.mdx -->

import { v4 as uuidv4 } from 'uuid'
import { Prism as SyntaxHighlighter } from 'react-syntax-highlighter'

import { Canvas, Meta, Story } from '@storybook/addon-docs'

import { Anchor } from '@a110/rito'

import styleClasses from './styles.module.css'

<Meta title="rito/component/Anchor" component={Anchor} />

# Anchor

The Anchor element provides a click interaction on its child, that
when clicked sets the window location to the specified `href` URL.

```tsx
<Paragraph>
  <Anchor href="survey.html">Click here</Anchor> to see the latest results!
</Paragraph>
```

Clicking the **color scheme button** in the toolbar ☝ will alter the color
of the link text.

<Canvas>
  <Story name="Visited">
    <div className={styleClasses.textBox}>
      <div className={styleClasses.textboxitem}>
        Here is{' '}
        <Anchor href={window.parent.location}>a link to this page</Anchor>, and
        of course, it's visited.
      </div>
      <div className={styleClasses.textboxitem}>
        Here is{' '}
        <Anchor href={`https://www.example.com/${uuidv4()}`}>
          a link to a random document
        </Anchor>
        , and as such it's not visited.
      </div>
    </div>
  </Story>
</Canvas>

export const Component = () => {
  return <SyntaxHighlighter />
}
Component.parameters = {
  docs: { source: { format: false } },
}
